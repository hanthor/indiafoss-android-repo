# Category definitions for fdroidserver (copied next to the generated config.yml).
# Keys must match the Categories used in metadata/*.yml.
Events:
  name: Events
Navigation:
  name: Navigation
Internet:
  name: Internet
